---
# enable snakemake for coordinating a pipeline action's workflow
# versions current as of January 2022
dependencies:
    - r-e1071  # provides support vector machines for adapter finding
    - r-igraph # provides graph analysis for junction and read clustering
    - r-rtsne  # provides clustering and visualization for svDJ, others
    - r-flexclust # provides weighted clustering for svDJ, others
