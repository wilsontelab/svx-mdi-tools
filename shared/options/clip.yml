---
# options used during node extraction and SV finding algorithms
options:
    min-clip: 
        order: 1
        short: c
        type: integer
        required: false
        default: 5
        description: "read clips shorter than --min-clip are not accepted as SV evidence"
