---
options:
    find-insertion-templates: 
        order: 1
        short: null
        type: boolean
        required: false
        default: false
        description: "if set, add columns to tables with the locations of locally templated insertions" 
    min-insertion-size: 
        order: 2
        short: null
        type: integer
        required: false
        default: 1
        description: "smallest insertion sequence to analyze" 
    max-insertion-size: 
        order: 3
        short: null
        type: integer
        required: false
        default: 20
        description: "largest insertion sequence to analyze" 
    min-template-size: 
        order: 4
        short: null
        type: integer
        required: false
        default: 7
        description: "require insertion templates plus flanking microhomologies to be at least this many bp" 
    insertion-search-space:
        order: 5
        short: null
        type: integer
        required: false
        default: 500
        description: "search for insertion templates within this many bp on each side of each breakpoint" 
